---
type: regex
weight: 1
pattern: '\d+\s*/\s*\d+'
target: last_message
---

Convergent output must carry an explicit `X/N` tally. This is mechanical, so it
is checked mechanically rather than spent on a judge — a deterministic check
cannot come back as a 2–1 split.
