---
type: tool_used
weight: 1
tool: Skill
input_match: fanout-investigation
min: 1
---

The cheap positive-trigger signal, and the only one that runs in CI.

Producing this matrix requires the skill body — the seven scope names, the tier
counts, the demotion ordering, and the matrix print format are all in `SKILL.md`
and none of them are in the `description` the model sees in its skill listing.
So a compliant answer here is evidence the skill actually loaded, and this
grader establishes it from the trace rather than inferring it from the output.
